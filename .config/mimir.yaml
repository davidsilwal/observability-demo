# Do not use this configuration in production. It is for demonstration purposes only.
# Simple Mimir configuration taken from https://grafana.com/docs/mimir/latest/get-started/
multitenancy_enabled: false

target: all,alertmanager,overrides-exporter

common:
  storage:
    backend: s3
    s3:
      endpoint: minio:9000
      insecure: true
      access_key_id: "CglZaOvJutYBwRJ72GJc"
      secret_access_key: "AAIqGaTs6Ro0TsLFJVCJN2fRhWK0m2UQ6lTFXuVI" 

blocks_storage:
  s3:
    bucket_name: mimir-blocks
  storage_prefix: blocks
  tsdb:
    dir: /data/ingester

alertmanager_storage:
  s3:
    bucket_name: mimir-alertmanager

ruler_storage:
  s3:
    bucket_name: mimir-ruler

limits:
  # Delete from storage metrics data older than 30 days.
  compactor_blocks_retention_period: 720h #30 days retention

# Use memberlist, a gossip-based protocol, to enable the 3 Mimir replicas to communicate
memberlist:
  join_members: [mimir]

ingester:
  ring:
    replication_factor: 1

server:
  http_listen_port: 9009
  log_level: error