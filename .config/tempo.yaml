server:
  http_listen_port: 3200

distributor:
  receivers:                           # this configuration will listen on all ports and protocols that tempo is capable of.
    otlp:
      protocols:
        grpc:  # OTLP gRPC receiver
          endpoint: "0.0.0.0:4317"
        http:  # OTLP HTTP receiver
          endpoint: "0.0.0.0:4318"


compactor:
    compaction:
      block_retention: 720h #30 days retention

storage:
  trace:
    backend: s3
    s3:
      endpoint: "minio:9000"
      bucket: grafana-tempo
      access_key: CglZaOvJutYBwRJ72GJc
      secret_key: AAIqGaTs6Ro0TsLFJVCJN2fRhWK0m2UQ6lTFXuVI
      insecure: true